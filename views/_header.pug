header.header
  nav.nav.nav--tours
    a.nav__el(href='/home') All Tours
  .header__logo
    a(href='/')
      img(src='/img/logo-white.png' alt='Natours Logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout(href='/api/v1/users/logout') Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a(href='/login').nav__el Login
      a(href='/signUp').nav__el.nav__el--cta Sign Up